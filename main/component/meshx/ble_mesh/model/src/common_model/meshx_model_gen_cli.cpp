/**
 * @file meshx_model_gen_cli.cpp
 * @brief Generic Client Model shim — DECOMMISSIONED (TASK-007, UVP migration).
 *
 * The Generic Client Model has been superseded by the Unified Vendor Protocol (UVP).
 * All outbound control messages are now sent via meshx_uvp_send() in the port layer.
 * This file is retained as a stub to avoid breaking the build system source glob.
 */

/* Intentionally empty — decommissioned in TASK-007 */
